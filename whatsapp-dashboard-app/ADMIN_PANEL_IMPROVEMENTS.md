# تحسينات لوحة تحكم الأدمن

## الميزات الجديدة المضافة

### 1. فلترة وترتيب متقدمة

#### للمستخدمين:
- **فلترة حسب الحالة**: نشط، موقوف، أدمن
- **ترتيب حسب**: الاسم، تاريخ الإنشاء، عدد الجلسات، آخر نشاط
- **بحث متقدم**: البحث في الاسم والبريد الإلكتروني

#### للجلسات:
- **فلترة حسب الحالة**: متصل، غير متصل، انتظار QR، منتهي الصلاحية
- **فلترة حسب المستخدم**: عرض جلسات مستخدم محدد
- **ترتيب حسب**: الاسم، تاريخ الإنشاء، انتهاء الصلاحية، الحالة
- **بحث متقدم**: البحث في اسم الجلسة، المستخدم، البريد الإلكتروني، معرف الجلسة

### 2. عرض تفاصيل المستخدم

عند النقر على زر "👁️ تفاصيل" في بطاقة المستخدم، يتم عرض:
- **معلومات المستخدم الأساسية**: الاسم، البريد، الحالة
- **مفاتيح API**: جميع مفاتيح API الخاصة بالمستخدم
- **توكنات الجلسات**: جميع توكنات الجلسات مع معرفات الجلسات
- **جلسات المستخدم**: قائمة بجميع جلسات المستخدم مع حالاتها

### 3. تحكم مباشر في الجلسات

من لوحة الأدمن، يمكنك:
- **إعادة تشغيل الجلسة**: إعادة تهيئة الجلسة
- **إيقاف الجلسة**: إيقاف الجلسة النشطة
- **حذف الجلسة**: حذف الجلسة نهائياً

### 4. تصدير البيانات

#### تصدير بيانات المستخدمين:
- تصدير إلى CSV
- يتضمن: الاسم، البريد، الحالة، الحد الأقصى للجلسات، أيام الجلسة، تاريخ الإنشاء، آخر نشاط

#### تصدير بيانات الجلسات:
- تصدير إلى CSV
- يتضمن: اسم الجلسة، المستخدم، البريد، الحالة، تاريخ الإنشاء، انتهاء الصلاحية، الأيام المتبقية، حالة الإيقاف

### 5. تحديث سريع

- **تحديث المستخدمين**: زر تحديث لتحميل أحدث بيانات المستخدمين
- **تحديث الجلسات**: زر تحديث لتحميل أحدث بيانات الجلسات

## كيفية الاستخدام

### فلترة المستخدمين:

1. استخدم حقل البحث للبحث في الأسماء أو البريد الإلكتروني
2. اختر حالة من قائمة "جميع الحالات"
3. اختر طريقة الترتيب من قائمة "الترتيب حسب"
4. انقر على "🔄 تحديث" لتحديث البيانات

### فلترة الجلسات:

1. استخدم حقل البحث للبحث في الجلسات
2. اختر حالة من قائمة "الحالة"
3. اختر مستخدم من قائمة "المستخدمين" (يتم تحديثها تلقائياً)
4. اختر طريقة الترتيب من قائمة "الترتيب حسب"

### عرض تفاصيل المستخدم:

1. ابحث عن المستخدم المطلوب
2. انقر على زر "👁️ تفاصيل" في بطاقة المستخدم
3. ستظهر نافذة منبثقة تحتوي على جميع التفاصيل

### تحكم في الجلسات:

1. ابحث عن الجلسة المطلوبة
2. انقر على زر "🎛️ تحكم"
3. اختر الإجراء المطلوب:
   - `1` لإعادة التشغيل
   - `2` للإيقاف
   - `3` للحذف

### تصدير البيانات:

1. في تبويب "المستخدمين" أو "الجلسات"
2. انقر على زر "📥 تصدير"
3. سيتم تحميل ملف CSV يحتوي على البيانات

## API Endpoints الجديدة

### GET `/api/admin/users/:userId/details`

الحصول على تفاصيل المستخدم الكاملة.

**Response:**
```json
{
  "success": true,
  "user": {
    "id": 1,
    "username": "user1",
    "email": "user1@example.com",
    "is_active": true,
    "is_admin": false,
    "max_sessions": 5,
    "session_ttl_days": 30,
    "created_at": "2024-01-01T00:00:00.000Z"
  },
  "apiKeys": [
    {
      "id": 1,
      "key_name": "API Key",
      "api_key": "xxx...",
      "created_at": "2024-01-01T00:00:00.000Z"
    }
  ],
  "sessionTokens": [
    {
      "id": 1,
      "session_id": "1",
      "token": "xxx...",
      "created_at": "2024-01-01T00:00:00.000Z"
    }
  ],
  "sessions": [
    {
      "id": 1,
      "session_name": "Session 1",
      "status": "connected",
      "created_at": "2024-01-01T00:00:00.000Z"
    }
  ]
}
```

## ملاحظات مهمة

1. **الأداء**: الفلترة والترتيب تتم على جانب العميل (Client-side) لسرعة الاستجابة
2. **التحديث**: استخدم أزرار التحديث للحصول على أحدث البيانات من الخادم
3. **التصدير**: الملفات المصدرة بصيغة CSV مع دعم UTF-8 للعربية
4. **الأمان**: جميع العمليات تتطلب صلاحيات أدمن

## التحسينات المستقبلية

- [ ] إضافة فلترة متقدمة (نطاق تواريخ، نطاق عدد الجلسات)
- [ ] إضافة تصدير JSON
- [ ] إضافة إحصائيات تفصيلية لكل مستخدم
- [ ] إضافة سجل نشاط المستخدمين
- [ ] إضافة إشعارات للمستخدمين من لوحة الأدمن

