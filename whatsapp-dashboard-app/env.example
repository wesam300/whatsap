# إعدادات البريد الإلكتروني
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# عنوان التطبيق
BASE_URL=http://localhost:3000

# مفتاح الجلسة (غير هذا في الإنتاج)
SESSION_SECRET=your-secret-key-change-this-in-production

# بيئة التشغيل (development أو production)
NODE_ENV=development

# ========================================
# إعدادات Reverse Proxy
# ========================================
# القيم الممكنة:
# - true: يثق في جميع proxies (افتراضي)
# - 1: يثق في أول proxy فقط
# - loopback: يثق في localhost proxies فقط
# - قائمة IPs مفصولة بفواصل: يثق في proxies محددة فقط
TRUST_PROXY=true

# ملاحظة: عند استخدام Nginx أو Cloudflare أو أي reverse proxy آخر
# تأكد من تكوين الـ proxy لإرسال headers التالية:
# - X-Forwarded-For: عنوان IP الحقيقي للعميل
# - X-Forwarded-Proto: البروتوكول المستخدم (http أو https)
# - X-Forwarded-Host: اسم الـ host الأصلي