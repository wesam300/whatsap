# كيفية تحديث النسخة على السيرفر

## 📋 الخطوات المطلوبة

### 1. الاتصال بالسيرفر

```bash
ssh root@your-server-ip
# أو
ssh username@your-server-ip
```

### 2. الانتقال إلى مجلد المشروع

```bash
cd /var/www/whatsap/whatsapp-dashboard-app
# أو المسار الصحيح لمشروعك
```

### 3. إيقاف الخادم (مهم!)

```bash
# إذا كنت تستخدم PM2
pm2 stop whatsapp-dashboard

# أو إذا كنت تستخدم screen/tmux
# اضغط Ctrl+C في نافذة الخادم

# أو إذا كان يعمل كخدمة
systemctl stop whatsapp-dashboard
```

### 4. جلب التحديثات من GitHub

```bash
# جلب آخر التحديثات
git pull origin main

# أو إذا كان لديك branch آخر
git pull origin your-branch-name
```

### 5. تثبيت/تحديث الحزم (إذا لزم الأمر)

```bash
npm install
```

### 6. تشغيل سكريبت إصلاح صلاحيات الأدمن (اختياري)

```bash
node fix-admin-permissions.js
```

### 7. تنظيف عمليات Chrome المتبقية (مهم!)

```bash
node cleanup-chrome-processes.js
```

### 8. إعادة تشغيل الخادم

```bash
# إذا كنت تستخدم PM2
pm2 restart whatsapp-dashboard

# أو إذا كنت تستخدم screen/tmux
node server.js

# أو إذا كان يعمل كخدمة
systemctl start whatsapp-dashboard
```

## 🔄 سكريبت تحديث تلقائي

يمكنك إنشاء سكريبت لتحديث تلقائي:

### إنشاء سكريبت التحديث

```bash
nano update-server.sh
```

أضف المحتوى التالي:

```bash
#!/bin/bash

echo "🔄 بدء تحديث السيرفر..."

# الانتقال إلى مجلد المشروع
cd /var/www/whatsap/whatsapp-dashboard-app

# إيقاف الخادم
echo "⏸️ إيقاف الخادم..."
pm2 stop whatsapp-dashboard || pkill -f "node server.js"

# جلب التحديثات
echo "📥 جلب التحديثات من GitHub..."
git pull origin main

# تثبيت الحزم
echo "📦 تثبيت/تحديث الحزم..."
npm install

# إصلاح صلاحيات الأدمن
echo "🔧 إصلاح صلاحيات الأدمن..."
node fix-admin-permissions.js

# تنظيف عمليات Chrome
echo "🧹 تنظيف عمليات Chrome المتبقية..."
node cleanup-chrome-processes.js

# إعادة تشغيل الخادم
echo "▶️ إعادة تشغيل الخادم..."
pm2 restart whatsapp-dashboard || node server.js

echo "✅ اكتمل التحديث بنجاح!"
```

### جعل السكريبت قابل للتنفيذ

```bash
chmod +x update-server.sh
```

### تشغيل السكريبت

```bash
./update-server.sh
```

## 📝 خطوات مفصلة (يدوي)

### الخطوة 1: التحقق من الحالة الحالية

```bash
# التحقق من حالة Git
git status

# التحقق من الخادم
pm2 status
# أو
ps aux | grep "node server.js"
```

### الخطوة 2: حفظ التغييرات المحلية (إن وجدت)

```bash
# إذا كان لديك تغييرات محلية غير محفوظة
git stash

# أو commit التغييرات
git add .
git commit -m "Local changes before update"
```

### الخطوة 3: جلب التحديثات

```bash
git fetch origin
git pull origin main
```

### الخطوة 4: التحقق من التحديثات

```bash
# عرض آخر commit
git log -1

# عرض الملفات المتغيرة
git diff HEAD~1
```

### الخطوة 5: تثبيت الحزم الجديدة

```bash
npm install
```

### الخطوة 6: إصلاح صلاحيات الأدمن

```bash
node fix-admin-permissions.js
```

### الخطوة 7: تنظيف عمليات Chrome

```bash
node cleanup-chrome-processes.js
```

### الخطوة 8: إعادة تشغيل الخادم

```bash
# PM2
pm2 restart whatsapp-dashboard

# أو إعادة تشغيل كامل
pm2 delete whatsapp-dashboard
pm2 start server.js --name whatsapp-dashboard
```

### الخطوة 9: التحقق من عمل الخادم

```bash
# عرض السجلات
pm2 logs whatsapp-dashboard

# أو
tail -f /var/log/whatsapp-dashboard.log
```

## 🔍 التحقق من نجاح التحديث

### 1. فحص حالة الخادم

```bash
pm2 status
```

يجب أن يكون الخادم في حالة `online`.

### 2. فحص السجلات

```bash
pm2 logs whatsapp-dashboard --lines 50
```

ابحث عن:
- ✅ `🚀 WhatsApp Dashboard Server running on port`
- ✅ `✅ تم إغلاق الجلسة بشكل كامل` (إذا كان هناك تنظيف)
- ❌ أي أخطاء

### 3. فحص الموقع

افتح المتصفح وتحقق من:
- ✅ الموقع يعمل
- ✅ يمكن تسجيل الدخول
- ✅ الجلسات تعمل بشكل صحيح

## ⚠️ استكشاف الأخطاء

### المشكلة: Git pull فشل

```bash
# إذا كان هناك تعارضات
git stash
git pull origin main
git stash pop
```

### المشكلة: npm install فشل

```bash
# حذف node_modules وإعادة التثبيت
rm -rf node_modules package-lock.json
npm install
```

### المشكلة: الخادم لا يعمل

```bash
# فحص الأخطاء
pm2 logs whatsapp-dashboard --err

# إعادة تشغيل كامل
pm2 delete whatsapp-dashboard
pm2 start server.js --name whatsapp-dashboard
```

### المشكلة: عمليات Chrome لا تغلق

```bash
# تشغيل السكريبت يدوياً
node cleanup-chrome-processes.js

# أو إغلاق يدوي
# Windows
taskkill /F /IM chrome.exe

# Linux/Mac
pkill -9 chrome
```

## 💡 نصائح

1. **احفظ نسخة احتياطية** قبل التحديث:
   ```bash
   cp -r whatsapp-dashboard-app whatsapp-dashboard-app-backup
   ```

2. **اختبر على بيئة تجريبية** أولاً إن أمكن

3. **راقب السجلات** بعد التحديث للتأكد من عدم وجود أخطاء

4. **استخدم PM2** لإدارة الخادم بشكل أفضل

## 📞 الدعم

إذا واجهت مشاكل:
1. راجع السجلات: `pm2 logs whatsapp-dashboard`
2. تحقق من حالة الخادم: `pm2 status`
3. راجع ملفات التوثيق في المشروع

