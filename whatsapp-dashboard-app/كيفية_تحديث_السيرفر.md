# 🚀 كيفية تحديث النسخة على السيرفر

## ⚡ الطريقة السريعة (موصى بها)

### 1. الاتصال بالسيرفر
```bash
ssh root@your-server-ip
```

### 2. الانتقال إلى مجلد المشروع
```bash
cd /var/www/whatsap/whatsapp-dashboard-app
```

### 3. تشغيل سكريبت التحديث التلقائي
```bash
chmod +x update-server.sh
./update-server.sh
```

**هذا كل شيء!** السكريبت سيقوم بـ:
- ✅ إيقاف الخادم
- ✅ جلب التحديثات من GitHub
- ✅ تثبيت الحزم الجديدة
- ✅ إصلاح صلاحيات الأدمن
- ✅ تنظيف عمليات Chrome
- ✅ إعادة تشغيل الخادم

---

## 📝 الطريقة اليدوية (خطوة بخطوة)

### الخطوة 1: إيقاف الخادم
```bash
pm2 stop whatsapp-dashboard
# أو
pkill -f "node server.js"
```

### الخطوة 2: جلب التحديثات
```bash
git pull origin main
```

### الخطوة 3: تثبيت الحزم
```bash
npm install
```

### الخطوة 4: إصلاح صلاحيات الأدمن
```bash
node fix-admin-permissions.js
```

### الخطوة 5: تنظيف عمليات Chrome
```bash
node cleanup-chrome-processes.js
```

### الخطوة 6: إعادة تشغيل الخادم
```bash
pm2 restart whatsapp-dashboard
# أو
pm2 start server.js --name whatsapp-dashboard
```

---

## 🔍 التحقق من نجاح التحديث

### 1. فحص حالة الخادم
```bash
pm2 status
```
يجب أن يكون الخادم في حالة `online` ✅

### 2. فحص السجلات
```bash
pm2 logs whatsapp-dashboard --lines 50
```
ابحث عن:
- ✅ `🚀 WhatsApp Dashboard Server running on port`
- ✅ `✅ تم إغلاق الجلسة بشكل كامل`
- ❌ لا توجد أخطاء

### 3. فحص الموقع
افتح المتصفح وتحقق من:
- ✅ الموقع يعمل
- ✅ يمكن تسجيل الدخول
- ✅ الجلسات تعمل

---

## ⚠️ استكشاف الأخطاء

### المشكلة: Git pull فشل
```bash
# حفظ التغييرات المحلية
git stash
git pull origin main
git stash pop
```

### المشكلة: npm install فشل
```bash
# حذف وإعادة تثبيت
rm -rf node_modules package-lock.json
npm install
```

### المشكلة: الخادم لا يعمل
```bash
# فحص الأخطاء
pm2 logs whatsapp-dashboard --err

# إعادة تشغيل كامل
pm2 delete whatsapp-dashboard
pm2 start server.js --name whatsapp-dashboard
```

---

## 💡 نصائح مهمة

1. **احفظ نسخة احتياطية** قبل التحديث:
   ```bash
   cp -r whatsapp-dashboard-app whatsapp-dashboard-app-backup
   ```

2. **راقب السجلات** بعد التحديث:
   ```bash
   pm2 logs whatsapp-dashboard --lines 100
   ```

3. **اختبر الموقع** بعد التحديث للتأكد من عمل كل شيء

---

## 📞 للمزيد من التفاصيل

راجع ملف: `UPDATE_SERVER.md`

