# كيفية تنظيف عمليات Chrome المتبقية

## 📋 المشكلة
بعد رفع التحديث، قد تبقى عمليات Chrome/Chromium تعمل في الخلفية للجلسات التي:
- انتهت صلاحيتها
- توقفت
- حُذفت

هذا يسبب استهلاك RAM غير ضروري.

## ✅ الحلول المتاحة

### 1. التنظيف التلقائي (مدمج)
الخادم يقوم بتنظيف تلقائي كل 30 دقيقة. **لا حاجة لتدخل يدوي** في معظم الحالات.

### 2. التنظيف اليدوي الفوري

#### أ) استخدام سكريبت Node.js (موصى به)

**في Windows:**
```bash
cd whatsapp-dashboard-app
node cleanup-chrome-processes.js
```

**في Linux/Mac:**
```bash
cd whatsapp-dashboard-app
node cleanup-chrome-processes.js
```

#### ب) استخدام سكريبت Shell/Batch

**في Windows:**
```bash
cleanup-chrome-processes.bat
```

**في Linux/Mac:**
```bash
chmod +x cleanup-chrome-processes.sh
./cleanup-chrome-processes.sh
```

## 🔧 ما يقوم به السكريبت

1. ✅ يفحص قاعدة البيانات للجلسات النشطة
2. ✅ يحدّث الجلسات المنتهية أو المنفصلة
3. ✅ يبحث عن عمليات Chrome/Chromium النشطة
4. ✅ يغلق العمليات المتبقية (التي لا تنتمي لجلسات نشطة)

## ⚠️ ملاحظات مهمة

- **آمن**: السكريبت يغلق فقط العمليات التي لا تنتمي لجلسات نشطة
- **الجلسات النشطة**: لن تتأثر بأي شكل من الأشكال
- **البيانات**: لا يتم حذف أي بيانات من قاعدة البيانات

## 📅 إضافة تنظيف تلقائي (اختياري)

### في Linux/Mac (Cron Job)

```bash
# فتح crontab
crontab -e

# إضافة السطر التالي (يستبدل المسار بالمسار الصحيح)
0 * * * * cd /var/www/whatsap/whatsapp-dashboard-app && node cleanup-chrome-processes.js >> /var/log/chrome-cleanup.log 2>&1
```

### في Windows (Task Scheduler)

1. افتح **Task Scheduler**
2. أنشئ مهمة جديدة
3. اضبطها لتشغيل `cleanup-chrome-processes.bat` كل ساعة

## 🔍 التحقق من النتيجة

بعد تشغيل السكريبت:

**في Windows:**
```bash
tasklist | findstr chrome.exe
```

**في Linux/Mac:**
```bash
ps aux | grep chrome
```

إذا لم تظهر أي عمليات Chrome (باستثناء الجلسات النشطة)، فالسكريبت نجح! ✅

## 🛠️ استكشاف الأخطاء

### المشكلة: السكريبت لا يغلق العمليات
- ✅ تأكد من تشغيل السكريبت كمسؤول (Administrator/Root)
- ✅ تحقق من أن المسار صحيح

### المشكلة: عمليات Chrome تعود بعد إغلاقها
- ✅ هذا طبيعي إذا كانت هناك جلسات نشطة
- ✅ الجلسات النشطة تفتح عمليات Chrome تلقائياً

## 💡 نصائح إضافية

1. **بعد حذف جلسة**: السكريبت التلقائي سينظف العمليات خلال 30 دقيقة
2. **بعد إيقاف جلسة**: يتم الإغلاق تلقائياً عبر `destroyClientCompletely`
3. **للتنظيف الفوري**: شغّل السكريبت اليدوي

## 📞 الدعم

إذا واجهت أي مشاكل، تحقق من:
- سجلات الخادم (console.log)
- ملف `CLEANUP_CHROME_PROCESSES.md` للتفاصيل التقنية

