# إصلاح مشاكل الاتصال بالجلسات وإدارة المستخدمين

## المشكلة 1: خطأ `getIsMyContact is not a function`

### الأعراض:
```
Error: Evaluation failed: TypeError: window.Store.ContactMethods.getIsMyContact is not a function
```

### السبب:
WhatsApp Web غير واجهة برمجة التطبيقات (API) الخاصة به، والدالة `getIsMyContact` لم تعد موجودة في بعض الإصدارات.

### الحل:

#### الطريقة 1: استخدام السكريبت التلقائي (مُوصى به)
```bash
cd /var/www/whatsap/whatsapp-dashboard-app
node fix-getIsMyContact.js
pm2 restart whatsapp-dashboard
```

#### الطريقة 2: التحديث اليدوي
إذا كان السكريبت لا يعمل، يمكنك تحديث الملف يدوياً:

1. افتح الملف:
```bash
nano node_modules/whatsapp-web.js/src/util/Injected/Utils.js
```

2. ابحث عن دالة `getContactModel` (حوالي السطر 635)

3. تأكد من وجود الكود التالي:
```javascript
// Helper function to safely get ContactMethods values with fallback
const safeGet = (methodName, fallback) => {
    try {
        if (window.Store.ContactMethods && typeof window.Store.ContactMethods[methodName] === 'function') {
            return window.Store.ContactMethods[methodName](contact);
        }
    } catch (e) {
        // Method doesn't exist, use fallback
    }
    return fallback !== undefined ? fallback : false;
};

res.isMyContact = safeGet('getIsMyContact', contact.isMyContact);
```

4. أعد تشغيل الخادم:
```bash
pm2 restart whatsapp-dashboard
```

### التحقق من الإصلاح:
بعد إعادة التشغيل، راقب السجلات:
```bash
pm2 logs whatsapp-dashboard
```

يجب ألا يظهر خطأ `getIsMyContact` بعد الآن.

---

## المشكلة 2: الجلسات لا تتصل بشكل صحيح

### الأعراض:
- الجلسات تفشل في الاتصال
- رسائل خطأ في السجلات
- الجلسات تبقى في حالة "loading" أو "waiting_for_qr"

### الحلول:

#### 1. تنظيف الجلسات القديمة
```bash
cd /var/www/whatsap/whatsapp-dashboard-app
node cleanup-chrome-processes.js
```

#### 2. إعادة تشغيل الجلسات
- من لوحة الأدمن: انتقل إلى "الجلسات" → اختر الجلسة → اضغط "إعادة التشغيل"
- أو من API:
```bash
curl -X POST http://localhost:3000/api/admin/sessions/{sessionId}/restart \
  -H "Cookie: session=YOUR_SESSION_COOKIE"
```

#### 3. حذف الجلسة وإنشائها من جديد
- من لوحة الأدمن: انتقل إلى "الجلسات" → اختر الجلسة → اضغط "حذف"
- ثم أنشئ جلسة جديدة من لوحة المستخدم

---

## إدارة المستخدمين من لوحة الأدمن

### الوظائف المتاحة:

#### 1. إيقاف/تفعيل المستخدم
- **الموقع**: لوحة الأدمن → المستخدمين
- **الطريقة**: اضغط على زر "⏸️ إيقاف" أو "▶️ تفعيل" بجانب المستخدم
- **التأثير**:
  - عند الإيقاف: يتم إغلاق جميع الجلسات النشطة للمستخدم تلقائياً
  - عند التفعيل: يمكن للمستخدم تسجيل الدخول واستخدام النظام

#### 2. حذف المستخدم
- **الموقع**: لوحة الأدمن → المستخدمين
- **الطريقة**: اضغط على زر "🗑️ حذف" بجانب المستخدم
- **التأثير**:
  - يتم حذف المستخدم نهائياً
  - يتم حذف جميع جلساته تلقائياً
  - يتم إلغاء تفعيل جميع مفاتيح API والتوكنات الخاصة به
  - **تحذير**: هذه العملية لا يمكن التراجع عنها!

#### 3. تعديل المستخدم
- **الموقع**: لوحة الأدمن → المستخدمين
- **الطريقة**: اضغط على زر "✏️ تعديل" بجانب المستخدم
- **يمكنك تعديل**:
  - اسم المستخدم
  - البريد الإلكتروني
  - كلمة المرور
  - الحد الأقصى للجلسات
  - مدة الجلسة (بالأيام)
  - صلاحيات الأدمن
  - حالة المستخدم (نشط/موقوف)

#### 4. عرض تفاصيل المستخدم
- **الموقع**: لوحة الأدمن → المستخدمين
- **الطريقة**: اضغط على زر "👁️ تفاصيل" بجانب المستخدم
- **يعرض**:
  - معلومات المستخدم الكاملة
  - الجلسات المرتبطة
  - مفاتيح API
  - سجل النشاط

#### 5. إدارة جلسات المستخدم
- **الموقع**: لوحة الأدمن → المستخدمين
- **الطريقة**: اضغط على زر "📱 الجلسات" بجانب المستخدم
- **يمكنك**:
  - عرض جميع جلسات المستخدم
  - إيقاف/تشغيل الجلسات
  - حذف الجلسات
  - تمديد مدة الجلسات

### ملاحظات مهمة:

1. **لا يمكنك حذف أو إيقاف حسابك الخاص**: النظام يمنع ذلك تلقائياً
2. **عند إيقاف المستخدم**: يتم إغلاق جميع جلساته النشطة تلقائياً
3. **عند حذف المستخدم**: يتم حذف جميع بياناته نهائياً (لا يمكن التراجع)
4. **النسخ الاحتياطي**: يُنصح بعمل نسخة احتياطية من قاعدة البيانات قبل حذف المستخدمين المهمين

---

## استكشاف الأخطاء

### المشكلة: الأزرار لا تعمل
**الحل**: 
1. تأكد من أنك مسجل دخول كأدمن
2. امسح ذاكرة التخزين المؤقت للمتصفح (Ctrl+Shift+Delete)
3. أعد تحميل الصفحة (F5)

### المشكلة: خطأ "المستخدم غير موجود"
**الحل**:
1. تأكد من أن المستخدم موجود في قاعدة البيانات
2. أعد تحميل قائمة المستخدمين
3. تحقق من السجلات في `pm2 logs whatsapp-dashboard`

### المشكلة: الجلسات لا تغلق عند إيقاف المستخدم
**الحل**:
1. شغل سكريبت التنظيف: `node cleanup-chrome-processes.js`
2. أعد تشغيل الخادم: `pm2 restart whatsapp-dashboard`
3. حاول إيقاف المستخدم مرة أخرى

---

## الدعم

إذا استمرت المشاكل:
1. تحقق من السجلات: `pm2 logs whatsapp-dashboard --lines 100`
2. تحقق من حالة الخادم: `pm2 status`
3. تحقق من قاعدة البيانات: `sqlite3 sessions/whatsapp_dashboard.db "SELECT * FROM users;"`

