# 🚀 دليل البدء السريع

## خطوات التثبيت

1. **تثبيت المكتبات:**
```bash
npm install
```

2. **إنشاء ملف الإعدادات:**
```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

3. **تعديل ملف `.env`:**
```env
PORT=3001
WHATSAPP_API_URL=http://localhost:3000/api
WHATSAPP_API_KEY=your_api_key_here
SESSION_TOKEN=your_session_token_here
SESSION_ID=your_session_id_here
```

4. **تشغيل السيرفر:**
```bash
# Windows
start.bat

# Linux/Mac
chmod +x start.sh
./start.sh

# أو مباشرة
npm start
```

5. **فتح المتصفح:**
افتح: `http://localhost:3001`

## 📝 استخدام سريع

### 1. تصميم تقرير جديد
- افتح الواجهة
- اضغط "تصميم تقرير"
- استخدم القالب المثال أو صمم تقريرك
- استخدم `{{اسم_المعامل}}` للمعاملات الديناميكية
- احفظ التقرير

### 2. إرسال تقرير
- اضغط "إرسال تقرير"
- اختر التقرير
- أدخل رقم الواتساب
- أدخل قيم المعاملات
- اضغط "إرسال"

### 3. استخدام API مباشرة

**إنشاء تقرير:**
```bash
POST /api/reports
{
  "name": "تقرير المبيعات",
  "template_data": {
    "html": "<html>...{{اسم_العميل}}...</html>"
  }
}
```

**إرسال تقرير:**
```bash
POST /api/reports/:id/send
{
  "phoneNumber": "966501234567",
  "parameters": {
    "اسم_العميل": "أحمد"
  }
}
```

**عرض تقرير:**
```
GET /api/reports/:id/view?اسم_العميل=أحمد&المبلغ=1000
```

## ⚠️ ملاحظات مهمة

1. تأكد من أن سيرفر الواتساب يعمل على المنفذ 3000
2. تأكد من أن الجلسة نشطة ومتصلة
3. استخدم API Key أو Session Token (ليس كلاهما)
4. رقم الهاتف يجب أن يكون بدون + أو مسافات

## 🔗 روابط مفيدة

- الواجهة: http://localhost:3001
- API: http://localhost:3001/api
- التقارير: http://localhost:3001/api/reports

