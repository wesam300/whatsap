# 📊 سيرفر التقارير - WhatsApp Reports Server

سيرفر منفصل لتصميم وإدارة التقارير مع إمكانية إرسالها عبر خدمة الواتساب.

## 🚀 المميزات

- ✅ تصميم تقارير HTML مخصصة
- ✅ استخدام معاملات ديناميكية في التقارير
- ✅ إرسال التقارير عبر الواتساب
- ✅ حفظ وإدارة التقارير
- ✅ سجلات إرسال كاملة
- ✅ واجهة عربية سهلة الاستخدام

## 📦 التثبيت

1. انتقل إلى مجلد المشروع:
```bash
cd reports-server
```

2. قم بتثبيت المكتبات:
```bash
npm install
```

3. انسخ ملف الإعدادات:
```bash
copy .env.example .env
```

4. عدّل ملف `.env` وأضف معلومات الواتساب:
```env
PORT=3001
WHATSAPP_API_URL=http://localhost:3000/api
WHATSAPP_API_KEY=your_api_key_here
SESSION_TOKEN=your_session_token_here
SESSION_ID=your_session_id_here
```

## 🎯 الاستخدام

### بدء السيرفر

```bash
npm start
```

أو للتطوير:
```bash
npm run dev
```

### الوصول للواجهة

افتح المتصفح على: `http://localhost:3001`

## 📝 استخدام API

### إنشاء تقرير جديد

```bash
POST /api/reports
Content-Type: application/json

{
  "name": "تقرير المبيعات",
  "description": "تقرير يومي للمبيعات",
  "template_data": {
    "html": "<html>...{{اسم_العميل}}...</html>",
    "css": "body { font-family: Arial; }"
  }
}
```

### عرض تقرير مع معاملات

```
GET /api/reports/:id/view?اسم_العميل=أحمد&المبلغ=1000
```

### إرسال تقرير عبر الواتساب

```bash
POST /api/reports/:id/send
Content-Type: application/json

{
  "phoneNumber": "966501234567",
  "parameters": {
    "اسم_العميل": "أحمد",
    "المبلغ": "1000"
  },
  "message": "تقرير المبيعات اليوم"
}
```

### الحصول على جميع التقارير

```bash
GET /api/reports
```

### الحصول على تقرير محدد

```bash
GET /api/reports/:id
```

### تحديث تقرير

```bash
PUT /api/reports/:id
Content-Type: application/json

{
  "name": "اسم جديد",
  "template_data": { ... }
}
```

### حذف تقرير

```bash
DELETE /api/reports/:id
```

### الحصول على سجلات الإرسال

```bash
GET /api/logs
```

## 🎨 تصميم التقارير

استخدم معاملات ديناميكية في HTML باستخدام الصيغة `{{اسم_المعامل}}`:

```html
<html>
<head>
    <style>
        body { font-family: Arial; padding: 20px; }
        h1 { color: #128C7E; }
    </style>
</head>
<body>
    <h1>تقرير: {{اسم_التقرير}}</h1>
    <p>العميل: {{اسم_العميل}}</p>
    <p>المبلغ: {{المبلغ}} ريال</p>
    <p>التاريخ: {{التاريخ}}</p>
</body>
</html>
```

عند الإرسال، سيتم استبدال `{{اسم_العميل}}` بالقيمة المرسلة في المعاملات.

## 🔗 التكامل مع سيرفر الواتساب

يجب أن يكون سيرفر الواتساب يعمل على المنفذ المحدد في `.env` (افتراضياً 3000).

تأكد من:
1. ✅ سيرفر الواتساب يعمل
2. ✅ لديك API Key أو Session Token صحيح
3. ✅ الجلسة نشطة ومتصلة

## 📁 هيكل المشروع

```
reports-server/
├── server.js          # السيرفر الرئيسي
├── package.json       # ملف التبعيات
├── .env.example       # مثال ملف الإعدادات
├── reports.db         # قاعدة البيانات (يتم إنشاؤها تلقائياً)
└── public/
    └── index.html     # واجهة المستخدم
```

## 🛠️ التطوير

للتطوير مع إعادة التحميل التلقائي:

```bash
npm run dev
```

## 📄 الترخيص

ISC

## 🤝 المساهمة

يمكنك تحسين المشروع وإضافة ميزات جديدة!

